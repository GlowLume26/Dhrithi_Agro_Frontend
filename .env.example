# Copy this to .env for local dev (already works via localhost fallback)
# For Netlify: set VITE_API_BASE in Site Settings → Environment Variables

VITE_API_BASE=https://your-backend-domain.com/index.php?route=
